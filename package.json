{
  "name": "checkout-e2e",
  "version": "1.0.0",
  "private": true,
  "description": "Black-box Playwright E2E suite for checkout-service (API + storefront UI)",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "report": "playwright show-report",
    "install:browsers": "playwright install chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0",
    "@types/node": "^22.0.0"
  }
}
